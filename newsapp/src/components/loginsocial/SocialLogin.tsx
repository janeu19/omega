// import React from 'react';

// const LoginSocial: React.FC = () => {
//     const handleLogin = (provider: string) => {
//         window.location.href = `/auth/${provider}`;
//     };

//     return (
//         <div>
//             <h1>Login</h1>
//             <button onClick={() => handleLogin('google')}>Login with Google</button>
//             <button onClick={() => handleLogin('github')}>Login with GitHub</button>
//             <button onClick={() => handleLogin('facebook')}>Login with Facebook</button>
//         </div>
//     );
// };

// export default LoginSocial;

// Documentation:
// This TypeScript React component provides buttons for logging in using Google, GitHub, or Facebook.
// Each button triggers a redirect to the respective OAuth route defined in the backend,
// which initiates the OAuth flow.
// The component is designed to facilitate easy integration and modification of additional authentication providers.
